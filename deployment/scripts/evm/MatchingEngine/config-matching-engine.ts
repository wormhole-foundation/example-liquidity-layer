import { runOnEvms, ChainInfo, LoggerFn } from "../../../helpers";
import { ethers } from "ethers";

runOnEvms("config-matching-engine", async (chain: ChainInfo, signer: ethers.Signer, log: LoggerFn) => {
  
});